# Tempest Carve demo

A local showcase for `markup-carve/tempest-carve`, covering the view component,
publishing profiles, extensions, caching, diagnostics, source-line annotations,
safe rendering, and every supported output format.

```sh
composer install
npm install
npm run build
php tempest serve
```

Run the full quality suite with `composer qa`.
